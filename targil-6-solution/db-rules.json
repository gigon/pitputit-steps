{
  "rules": {
    ".read": true,
    "messages": {
      "$messageKey": {
        ".write": "auth.provider == 'google' 
               && newData.child('userKey').val() == auth.uid"     
      }
    }
  }
}
